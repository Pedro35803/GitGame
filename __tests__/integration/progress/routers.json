[
  {
    "router": "playerProgress",
    "data": {
      "id_capter": "{{id_capter}}",
      "id_user": "{{id_player}}"
    }
  },
  {
    "router": "capterProgress",
    "data": {
      "id_player_progress": "{{id_playerProgress}}",
      "id_level": "{{id_level}}"
    }
  },
  {
    "router": "levelProgress",
    "data": {
      "id_capter_progress": "{{id_capterProgress}}",
      "id_orderLevel": "{{id_orderLevel}}"
    }
  }
]
